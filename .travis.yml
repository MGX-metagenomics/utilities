notifications:
  email:
    recipients:
      - sebastian@jaenicke.org

language: java

dist: trusty

sudo: required

jdk: oraclejdk8

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.m2

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq protobuf-compiler

script:
  - mvn clean install
